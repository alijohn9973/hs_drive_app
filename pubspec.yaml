name: hs_drive_full
description: HS Drive - Rider + Driver Flutter App (placeholder scaffold)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.4.0
  geolocator: ^9.0.2
  permission_handler: ^10.2.0
  dio: ^5.0.0
  socket_io_client: ^2.0.0
  flutter_secure_storage: ^8.0.0
  provider: ^6.0.5
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true